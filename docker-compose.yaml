version: '3.6'
services:
  multisma2:
    container_name: multisma2
    image: multisma2:latest
    restart: unless-stopped
    volumes:
    - ./volumes/multisma2/log:/solar/log
    - ./volumes/multisma2/data:/solar/data
    depends_on:
    - mosquitto
    